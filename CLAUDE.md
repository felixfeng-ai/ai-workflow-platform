# CLAUDE.md

VeyaWork 雅秩 —— AI 原生工作台（FastAPI + React + Taro 小程序）。
项目全景见 [docs/platform-overview.md](docs/platform-overview.md)，快速开始见 [README.md](README.md)。

---

## 协作约定：反向讲解

**用户主栈是前端，后端与基础设施是 AI 代写的。因此本项目采用「反向讲解」模式，防止出现"代码写完了但人讲不清"的情况。**

### 每次交付代码后，必须附一组「为什么」问题

- 出 **3–5 个**问题，聚焦本次改动里的**设计取舍**，不是"这个函数做了什么"
- 只问「为什么这么选 / 为什么不选另一种 / 什么情况下会坏」
- 用户答完后，**逐条判定**：讲对了 / 讲漏了 / 讲错了，并补上漏掉的那一层
- 讲不出来的地方**如实指出**，不要放过 —— 那是面试会穿帮的地方，也是这个机制存在的唯一理由

### 什么样的问题算合格

| ❌ 不要问 | ✅ 要问 |
| --- | --- |
| `require_role` 是做什么的？ | 为什么越权访问返回 404 而不是 403？泄漏了什么信息？ |
| 这里用了 SSE | 为什么选 SSE 而不是 WebSocket？代价是什么？ |
| 限流器是进程内的 | 什么部署方式下这个限流会失效？为什么现在敢这么做？ |

### 交付时同时说明

- **否掉了什么方案**，以及否掉的理由（用户面试时最需要讲的正是这个）
- **这段代码在哪个文件**，给出可点击路径

### 不要做的事

- 不要为了解释而写长篇文档 —— 用户要的是**能讲出来**，不是能查到
- 不要问用户"要不要继续"之类的流程确认 —— 用户明确要求一次性做完，确认环节只保留"理解"这一环
- 不要在用户答错时直接给答案 —— 先指出错在哪一层，让他自己补

---

## 语言与注释

- **所有回复一律中文**（子代理的英文输出先翻译再呈现）
- **后端非显然写法必须加中文注释**：依赖注入、生命周期、租户隔离点、并发/竞态、为何不用某个更直觉的写法
- 前端注释克制，只在有反直觉之处写

## 测试

- **只跑增量测试**：改哪测哪（`pytest tests/test_xxx.py`），不要每次跑全量
- 新功能沿用 TDD：先写失败测试再实现
- 测试基线：21 个文件 / 225 个用例，见 CI 双跑（内存 SQLite 单测 + 真实 PostgreSQL 集成测试）

---

## 硬性不变量（改代码时必须守住）

1. **多租户行级隔离** —— 所有查询必须带 `tenant_id`；跨租户与不存在**一律返回 404**，不返回 403（不泄漏资源是否存在）
2. **Alembic 是唯一建表来源** —— 禁止在应用代码里加 `create_all`
3. **后台任务自开会话** —— 智能体与工作流执行器各自持有独立 session，绝不共享请求级会话
4. **调度器是全进程单例** —— 多 worker 部署前必须换分布式锁，否则同一任务重复触发
5. **AI 产出统一走 Markdown 渲染** —— 前端只有一个 `<Markdown>` 入口

## 部署约束

- 纯文档改动（`**.md` / `docs/**` / `design-previews/**`）不触发生产部署（`.github/workflows/deploy.yml` 的 `paths-ignore`）
- 访问链路：`veyawork.work` → 香港反代 → 阿里云 8080；香港侧须把 `Host` 改写成上游 `IP:8080` 以规避阿里云按 Host 的 ICP 拦截
- 细节见 [DEPLOYMENT.md](DEPLOYMENT.md)

## 仅本地、禁止推送

- `INTERVIEW_PREP.md`（面试准备稿）由 `.git/info/exclude` 排除，**不进版本库、不推送**
